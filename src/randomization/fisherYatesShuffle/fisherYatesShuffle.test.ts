import { fisherYatesShuffle } from "./fisherYatesShuffle";

test("view the randomized arrays", () => {
  const arr = [1, 2, 3, 4, 5, 6];
  const random = fisherYatesShuffle(arr);
  expect(true).toBe(false);
});
